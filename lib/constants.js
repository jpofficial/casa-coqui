// ─── Shared constants ─────────────────────────────────────────────────────────
// Centralized so API and UI stay in sync.

/** Maximum total members per booking (primary guest + invited companions). */
export const MAX_MEMBERS = 10;
