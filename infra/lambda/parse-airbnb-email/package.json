{
  "name": "parse-airbnb-email",
  "version": "2.0.0",
  "description": "Lambda handler: reads raw Airbnb emails from S3, classifies them, matches to a booking, and writes to Firestore airbnb_messages.",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.89.0",
    "@aws-sdk/client-s3": "^3.600.0",
    "@aws-sdk/client-secrets-manager": "^3.600.0",
    "firebase-admin": "^12.0.0",
    "mailparser": "^3.7.1"
  },
  "scripts": {
    "test": "jest",
    "test:load": "node -e \"require('./index'); console.log('module loads OK')\""
  },
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "jest": "^30.3.0"
  }
}
